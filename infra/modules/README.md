# Modules

Reusable infrastructure modules belong in this directory.

## Current Modules

- `ai/foundry.bicep`: Azure AI Foundry contract module with `attach` and `create` modes plus a normalized output surface for the project endpoint, memory-store contract, and default model deployment contract.
