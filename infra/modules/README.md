# Modules

Reusable Bicep modules are grouped by responsibility:

- `foundation`: shared runtime prerequisites
- `data`: operational storage and secret-management resources
- `ai`: Foundry wiring and deployment contract normalization
- `hosting`: Container App and Container Apps Job wrappers for each workload
- `channel`: public edge and webhook-related outputs
- `observability`: baseline diagnostics
